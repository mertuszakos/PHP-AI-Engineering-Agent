services:
  php:
    build: .
    container_name: ai-agent-lab
    working_dir: /var/www
    volumes:
      - ./:/var/www
      - ../test-project:/var/www/workspace
    env_file:
      - .env
    stdin_open: true
    tty: true